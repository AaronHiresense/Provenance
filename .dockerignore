frontend/node_modules/
__pycache__/
.pytest_cache/
.git/
mca.duckdb
*.duckdb

# never bake secrets into the image: llm.py gives .env precedence OVER
# ambient env vars, so a copied .env would silently override the platform's
# configured variables (Railway, Fly, etc.).
.env
