// Loaded lazily by <LazyMotion> in main.tsx. domMax carries layout / layoutId
// animations (the selection ring, expanding rows) on top of domAnimation.
export { domMax as default } from "motion/react";
